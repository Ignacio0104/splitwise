import { UserCredential } from "firebase/auth";

export const mockUserCrendentials = {
  user: {
    displayName: "Nacho",
    email: "nacho@gmail.com",
    emailVerified: true,
  },
} as UserCredential;
